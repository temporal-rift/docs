---
title: "Read Service API"
description: "Game state, history, and probability query endpoints"
---

`read-service` serves denormalized, per-player views of game state. This is the primary endpoint clients use on load and reconnect.

## Get full game state

```http
GET /api/v1/games/{gameId}/state
```

Full per-player view — no raw probabilities, no other players' factions.

**Response `200`:**
```json
{
  "gameId": "uuid",
  "eraNumber": 2,
  "phase": "ACTION_ROUND_2",
  "myFaction": "ERASERS",
  "myHand": [
    { "cardInstanceId": "uuid", "cardType": "PUSH", "grade": "II", "isPlayableThisRound": true },
    { "cardInstanceId": "uuid", "cardType": "SCAN", "grade": "I", "isPlayableThisRound": false }
  ],
  "myScore": 12,
  "mySpecialActions": ["ANNIHILATE", "CORRUPT", "CASCADE"],
  "myJammedUntilRound": null,
  "myRevealedIntel": [
    {
      "kind": "PROBABILITY",
      "observedInRound": 2,
      "eventId": "uuid",
      "outcomes": [
        { "outcomeId": "uuid", "probability": 45, "isAnnihilated": false, "isSealed": false }
      ]
    }
  ],
  "activeEvents": [
    {
      "eventId": "uuid",
      "title": "string",
      "carryOverState": "FRESH",
      "outcomes": [
        { "outcomeId": "uuid", "description": "string" },
        { "outcomeId": "uuid", "description": "string" },
        { "outcomeId": "uuid", "description": "string" }
      ]
    }
  ],
  "players": [
    { "playerId": "uuid", "playerName": "string", "score": 8, "isConnected": true, "faction": null }
  ],
  "lastRoundSummary": {
    "roundNumber": 1,
    "actionSummaries": [
      { "playerId": "uuid", "actionCategory": "PROBABILITY_SHIFTER", "actionFamily": "CARD", "skipped": false }
    ]
  }
}
```

Every active event has required `carryOverState`: `FRESH`, `CASCADED`, or `STALLED`.

<Note title="Faction visibility">
`faction` on other players is `null` until `FactionRevealed` fires at game end. Your own faction is always visible in `myFaction`.
</Note>

### Hand fields

`grade` is always present — single-grade cards carry `I`. `isPlayableThisRound` tells the client which cards to grey
out; it changes as rounds advance without a new hand being dealt.

### Revealed intel

`myRevealedIntel` holds what you have bought with Information cards **this era**, and is emptied when the era ends.
Each entry carries `observedInRound` so the client can show how fresh it is.

| `kind` | From | Contents |
|---|---|---|
| `PROBABILITY` | Scan | exact outcome values for one event, refreshed every round until era end |
| `INFLUENCE` | Trace | which players acted on one event last round |
| `HAND_CARD` | Intercept | a card seen in another player's hand |

<Note title="This is the only source of exact probabilities">
Scan results reach you here and over the WebSocket — there is no probability endpoint. Every other player sees only
Low/Medium/High bands after Action Round 2.
</Note>

## Timeline service endpoints

### Get Weaver chain

```http
GET /api/v1/games/{gameId}/chains
```

<Warning title="Restricted">
Only accessible to the player assigned the Weavers faction.
</Warning>


**Response `200`:**
```json
{
  "chainId": "uuid",
  "status": "ACTIVE",
  "chainLength": 2,
  "links": [
    { "eventId": "uuid", "outcomeId": "uuid", "eraNumber": 1 },
    { "eventId": "uuid", "outcomeId": "uuid", "eraNumber": 2 }
  ]
}
```

`status`: `ACTIVE`, `COMPLETED`, or `BROKEN`
