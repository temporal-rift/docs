---
title: "Events & Kafka"
description: "Topics, envelope schema, privacy rules, and schema evolution"
---

All cross-service communication flows through Kafka. There are four topics in total, each scoped to a bounded context and partitioned by `gameId` to guarantee in-order delivery for every game.

## Topics

| Topic | Producer | Consumers | Partition key |
|---|---|---|---|
| `game.events` | game-service | timeline-service, read-service | `gameId` |
| `timeline.events` | timeline-service | game-service, read-service | `gameId` |
| `game.commands` | *(no in-workspace producer)* | game-service (`PlayerReconnectKafkaConsumer`) | `lobbyId` or `gameId` |
| `game.dlq` | game-service (`KafkaErrorHandlingConfig` dead-letter target after retries are exhausted) | — | `gameId` |

All four topics are created with three partitions and no explicit `retention.ms` override, so retention follows the
broker default rather than a differentiated per-topic policy.

<Note>
There is **no `outbox.events` Kafka topic**. The transactional outbox is the Spring Modulith `event_publication` database table — a background relay reads it and publishes directly to the domain topics above. See [Outbox Pattern](/architecture/outbox).
</Note>

## Event envelope

Every event carries the same set of envelope fields as **Kafka message headers**, with the event body as the message
payload. The event contract is defined spec-first as [AsyncAPI](https://www.asyncapi.com/), and services generate their
producer and payload types from the published event specifications. Consumers route on the `eventType` header, whose
value is the message name exactly as the specification declares it.

| Header | Description |
|---|---|
| `eventId` | Unique per event instance. Used for idempotency. |
| `aggregateId` | ID of the aggregate that produced this event |
| `aggregateType` | Type of aggregate e.g. `Lobby`, `FutureEvent` |
| `gameId` | Always present — Kafka partition key |
| `occurredAt` | ISO-8601 timestamp |
| `version` | Schema version. Unknown versions are logged and skipped — consumers never crash on unrecognised schemas. |

The event *type* is the AsyncAPI message identity, not a separate string field.

## Privacy rules

| Symbol | Meaning |
|---|---|
| 🌐 | **Public** — delivered to all players in the game |
| 🔒 | **Private** — delivered only to the target player |
| 📢 | **Filtered** — delivered to all but with fields redacted per visibility rules |

The `notification` module in `read-service` enforces these rules before forwarding events to WebSocket subscribers. A privacy leak — for example, delivering `CardPlayed` to all players — would expose a player's strategy and break the game.

`ActivistDeclarationRecorded`, `ExposeSignatureRevealed`, and `ExposeBehaviorChanged` are deliberately filtered public
projections: forward only their documented payloads. Do not derive a public notification from `SpecialActionPlayed`, and
never include an Expose target's Round-3 signature.

## Schema evolution

Additive changes — new optional fields — increment `version` and remain backwards compatible. Breaking changes produce a new event type; the old type is deprecated with a sunset date.

Consumers are expected to ignore unknown fields and skip events with unrecognised `version` values with a log entry. This keeps the system forward-compatible without coordinated deploys.
