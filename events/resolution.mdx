---
title: "Resolution Events"
description: "Events exchanged during outcome and paradox resolution"
---

Resolution lifecycle events are produced by `timeline-service` except `ParadoxResolutionCardPlayed`, which is the
private player-submission fact produced by `game-service` on `game.events` and consumed by timeline-service.

## ResolutionStarted 🌐

```json
{ "gameId": "uuid", "eraNumber": 1 }
```

## ProbabilityStateCalculated 📢

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "eventStates": [{
    "eventId": "uuid",
    "outcomes": [
      { "outcomeId": "uuid", "probability": 45, "isAnnihilated": false, "isSealed": false },
      { "outcomeId": "uuid", "probability": 35, "isAnnihilated": false, "isSealed": false },
      { "outcomeId": "uuid", "probability": 20, "isAnnihilated": true,  "isSealed": false }
    ]
  }]
}
```

The result is private to the player who played `SCAN` this round. WebSocket delivery of
`ProbabilityStateCalculated` is deferred beyond MVP 3, so MVP 3 forwards it to nobody.

## BandedProbabilityPublished 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "eventStates": [{
    "eventId": "uuid",
    "outcomes": [
      { "outcomeId": "uuid", "band": "HIGH" },
      { "outcomeId": "uuid", "band": "MEDIUM" },
      { "outcomeId": "uuid", "band": "LOW" }
    ]
  }]
}
```

Published to all players after Action Round 2 closes. Bands (`LOW` 0–30%, `MEDIUM` 31–60%, `HIGH` 61–90%) are derived from cumulative probability state of Rounds 1 and 2. Exact values remain private to the player who used `SCAN`; their WebSocket delivery is deferred beyond MVP 3.

## ParadoxDetected 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "paradoxes": [{
    "paradoxId": "uuid",
    "type": "DEAD_HEAT",
    "affectedEventId": "uuid",
    "affectedOutcomeIds": ["uuid", "uuid"],
    "description": "string"
  }]
}
```

## ParadoxResolutionPhaseStarted 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "paradoxIds": ["uuid"],
  "timerSeconds": 60
}
```

## ParadoxResolutionCardPlayed 🔒

Published by `game-service`'s action module to `game.events`; consumed by `timeline-service`. It is never published on
`timeline.events` and is never forwarded to players.

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "playerId": "uuid",
  "cardInstanceId": "uuid",
  "cardType": "STABILIZE",
  "targetEventId": "uuid",
  "targetOutcomeId": "uuid"
}
```

No faction special actions are allowed during paradox resolution. Eligible cards are `PUSH`, `SUPPRESS`, `STABILIZE`,
and `DETONATE`; the phase-scoped event has no `roundNumber`.

## ParadoxResolved 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "paradoxId": "uuid",
  "resolvedByPlayerId": "uuid"
}
```

## ParadoxCascaded 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "paradoxId": "uuid",
  "affectedEventId": "uuid",
  "carryForwardProbabilityState": [
    { "outcomeId": "uuid", "probability": 50 },
    { "outcomeId": "uuid", "probability": 50 },
    { "outcomeId": "uuid", "probability": 0 }
  ],
  "detonatedByPlayerIds": ["uuid"]
}
```

`detonatedByPlayerIds` lists every player, if any, whose `DETONATE` card targeted this cascade — empty when none
did. Multiple `DETONATE`s on the same cascade don't stack. This is a fact only; the doubled score penalty for
every other player is applied by scoring, not carried in this event.

## CorruptInversionConfirmed 🔒

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "roundNumber": 2,
  "corruptingPlayerId": "uuid",
  "targetEventId": "uuid",
  "targetOutcomeId": "uuid",
  "tookEffect": true
}
```

Service-coordination data, not a standalone player notification. Confirms whether a round's `CORRUPT` actually
inverted its target card's effect, or was blocked by a same-round `SEAL` (`tookEffect: false`).

## ResolutionFailed 🔒

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "affectedEventId": "uuid",
  "reason": "PROBABILITY_SUM_INVALID"
}
```

Internal-only: signals an invalid probability state after resolution. Not expected in normal play.

## ResolutionWarning 🔒

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "roundNumber": 2,
  "detail": "string"
}
```

Internal-only: logs a non-fatal ordering ambiguity that was resolved deterministically. No player-visible effect.

## OutcomeApplied 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "eventId": "uuid",
  "winningOutcomeId": "uuid",
  "finalProbabilities": [
    { "outcomeId": "uuid", "probability": 65 },
    { "outcomeId": "uuid", "probability": 25 },
    { "outcomeId": "uuid", "probability": 10 }
  ]
}
```

## EraResolutionCompleted 🔒

This is service-coordination data, not a standalone player notification. Timeline emits it exactly once after all
resolvable `OutcomeApplied` and `ParadoxCascaded` facts for the era. A `STALL` card places its selected event in the
barrier as `STALLED`; it emits neither an outcome fact nor a paradox fact. A paradox-resolution timeout instead
force-cascades unresolved paradoxes. Entries are in the original `EventsDrawn.events` reveal order, including
carried-over events.

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "terminalResolutions": [
    { "eventId": "uuid", "revealIndex": 0, "terminalState": "OUTCOME_APPLIED", "winningOutcomeId": "uuid" },
    { "eventId": "uuid", "revealIndex": 1, "terminalState": "STALLED" }
  ]
}
```

`revealIndex` and array index match the zero-based position in `EventsDrawn.events`; producers and consumers validate
that every active event appears exactly once in that order. Session uses `CASCADED` entries to determine the
collapsing event, carries both `CASCADED` and `STALLED` entries into the next era, and keeps a stalled event out of
collapse accounting. Scoring finishes either non-applied state without waiting for an outcome that cannot arrive.

## ScoresUpdated 🌐

```json
{
  "gameId": "uuid",
  "eraNumber": 1,
  "updates": [{
    "playerId": "uuid",
    "faction": "PROPHETS",
    "pointsDelta": 4,
    "reason": "EVENT_RESOLVED_AS_WRITTEN",
    "newTotal": 12
  }]
}
```
